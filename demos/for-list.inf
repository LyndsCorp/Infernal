#!../infernal
# Variables, listas, funciones y control de flujo.
list nombres = ["Abracitos", "Linus", "Richard", "David"]

function saludar(string nombre)
    return "Hola, " + nombre
fi

for nombre in nombres then
    print(saludar(nombre))
fi
